{
  "name": "blockdistraction-extension",
  "version": "4.3.3",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "validate": "node tools/validate-extension.js",
    "check": "npm run test && npm run validate"
  }
}
